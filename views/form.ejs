<!doctype html>
<html lang="ro">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>Formular Inscriere</title>
    <link rel="stylesheet" href="/styles.css" />
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link href="https://fonts.googleapis.com/css2?family=Manrope:wght@400;600;700&display=swap" rel="stylesheet" />
  </head>
  <body>
    <header class="header">
      <div class="header-inner">
        <div class="logo-stack">
          <img src="/img/alphard.png" alt="Alphard" />
          <img src="/img/advantage.png" alt="British Council" />
          <img src="/img/alphard-white.png" alt="Alphard Logo" />
        </div>
        <div class="brand-title">Alphard Educational Centre</div>
      </div>
    </header>

    <main class="container">
      <section class="hero">
        <h1>Formular de inscriere pentru examene Cambridge</h1>
        <p>Completeaza datele elevului pentru a primi detalii despre pregatirea examenului.</p>
      </section>

      <section class="form-card">
        <% if (success) { %>
          <div class="notice">Cererea a fost trimisa cu succes. Multumim!</div>
        <% } %>
        <% if (typeof error !== 'undefined' && error) { %>
          <div class="notice error"><%= error %></div>
        <% } %>

        <form action="/submit" method="post" class="grid">
          <div>
            <label for="first_name">Nume</label>
            <input id="first_name" name="first_name" required />
          </div>
          <div>
            <label for="last_name">Prenume</label>
            <input id="last_name" name="last_name" required />
          </div>
          <div>
            <label for="birth_date">Data de nastere</label>
            <input id="birth_date" name="birth_date" type="date" required />
          </div>
          <div>
            <label for="cnp">CNP (cod numeric personal)</label>
            <input id="cnp" name="cnp" inputmode="numeric" pattern="[0-9]{13}" maxlength="13" required />
          </div>
          <div>
            <label for="phone">Numar de telefon</label>
            <input id="phone" name="phone" type="tel" required />
          </div>
          <div>
            <label for="email">Adresa de e-mail</label>
            <input id="email" name="email" type="email" required />
          </div>
          <div>
            <label for="exam">Tip examen</label>
            <select id="exam" name="exam" required>
              <option value="" disabled selected>Alege examenul</option>
              <% exams.forEach(exam => { %>
                <option value="<%= exam %>"><%= exam %></option>
              <% }) %>
            </select>
          </div>
          <div class="full">
            <button class="btn" type="submit">Trimite cererea</button>
          </div>
        </form>
        <div class="footer">Daca ai intrebari, te rugam sa contactezi profesoara de engleza.</div>
      </section>
    </main>
  </body>
</html>
